import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

// next.config.js
module.exports = {
  images: {
    domains: ['static1.squarespace.com', 'media.licdn.com', 'avatar.vercel.sh'],
  },
};

export default nextConfig;
